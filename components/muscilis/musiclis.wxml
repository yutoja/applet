<view class="musciList">
<view class="musciListhe">播放列表</view>
<scroll-view scroll-y class="musicscroll" id="{{music}}">
<view class="musicv" wx:for="{{list}}" >
<view class="red" wx:if='{{item.id==music}}'></view>
<view class="lieb over" bindtap="skip" id="{{item.id}}" data-dt="{{item.dt}}">{{item.name}} - <text class="gesh">{{item.ar[0].name}}</text></view>
<view class="iconfont icon-guanbi" bindtap="remov" id="{{item.id}}">X</view>
</view>
</scroll-view>
</view>

